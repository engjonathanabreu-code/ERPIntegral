(() => {
'use strict';
if(window.ERPMetaProjetos)return;window.ERPMetaProjetos=true;
let bridge=null,sb=null,processos=[],currentMetaId=null,currentProcessId=null;
const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
async function loadProcessos(){if(!sb)return;const r=await sb.from('processos_kanban').select('id,crm_projeto_id,municipio,estado,nucleo,excluido_erp').eq('excluido_erp',false).order('municipio').order('nucleo');if(!r.error)processos=r.data||[];else console.warn('Projetos para metas',r.error);}
async function syncMeta(metaId){if(!metaId||!sb)return;try{const {error}=await sb.functions.invoke('sincronizar-meta-crm',{body:{meta_id:metaId}});if(error)console.warn('Sincronização meta CRM',error)}catch(e){console.warn('Sincronização meta CRM',e)}}
async function existingLinks(metaId){if(!metaId)return[];const r=await sb.from('meta_projetos').select('processo_id').eq('meta_id',metaId);return r.error?[]:(r.data||[]).map(x=>x.processo_id)}
function groupedOptions(selected=[]){
  const groups=new Map();
  processos.forEach(p=>{const municipio=String(p.municipio||'Sem município').trim()||'Sem município';if(!groups.has(municipio))groups.set(municipio,[]);groups.get(municipio).push(p)});
  return [...groups.entries()].map(([m,ps])=>{
    const selectedCount=ps.filter(p=>selected.includes(p.id)).length;
    const opened=selectedCount>0;
    return `<section class="meta-project-group ${opened?'is-open':''}" data-municipio="${esc(m)}">
      <button type="button" class="meta-project-tab" aria-expanded="${opened?'true':'false'}">
        <span class="meta-project-tab-name">${esc(m)}</span>
        <span class="meta-project-tab-meta"><span class="meta-project-selected-count" ${selectedCount?'':'hidden'}>${selectedCount} selecionado${selectedCount===1?'':'s'}</span><span class="meta-project-total">${ps.length}</span><span class="meta-project-chevron">⌄</span></span>
      </button>
      <div class="meta-project-panel" ${opened?'':'hidden'}>
        ${ps.map(p=>`<label class="meta-project-item"><input type="checkbox" name="metaProjeto" value="${p.id}" ${selected.includes(p.id)?'checked':''}><span>${esc(p.nucleo)}</span></label>`).join('')}
      </div>
    </section>`;
  }).join('')
}
function updateGroupCount(group){if(!group)return;const n=group.querySelectorAll('input[name="metaProjeto"]:checked').length;const badge=group.querySelector('.meta-project-selected-count');if(badge){badge.hidden=!n;badge.textContent=n?`${n} selecionado${n===1?'':'s'}`:''}}
function setGroupOpen(group,open){if(!group)return;group.classList.toggle('is-open',open);group.querySelector('.meta-project-panel')?.toggleAttribute('hidden',!open);const btn=group.querySelector('.meta-project-tab');if(btn)btn.setAttribute('aria-expanded',open?'true':'false')}
function wireProjectTabs(wrap){
  wrap.querySelectorAll('.meta-project-tab').forEach(btn=>btn.addEventListener('click',()=>{const group=btn.closest('.meta-project-group');setGroupOpen(group,!group.classList.contains('is-open'))}));
  wrap.querySelectorAll('input[name="metaProjeto"]').forEach(input=>input.addEventListener('change',()=>updateGroupCount(input.closest('.meta-project-group'))));
}
async function enhanceForm(form){if(form.dataset.projectEnhanced)return;form.dataset.projectEnhanced='1';await loadProcessos();const selected=[...new Set([...(await existingLinks(currentMetaId)),...(currentMetaId||!currentProcessId?[]:[currentProcessId])])];const respField=[...form.querySelectorAll('.field.full')].find(x=>x.querySelector('input[name="responsavel"]'));
 const linkedProcess=currentProcessId?processos.find(p=>p.id===currentProcessId):null;const wrap=document.createElement('div');wrap.className='field full meta-project-link-field';wrap.innerHTML=`<label>Projetos / Núcleos vinculados</label>${linkedProcess?`<div class="notice meta-process-linked">Esta meta já está vinculada a: <b>${esc(linkedProcess.municipio)} · ${esc(linkedProcess.nucleo)}</b></div>`:''}<input type="search" class="meta-project-search" placeholder="Buscar município ou núcleo"><div class="meta-project-picker">${groupedOptions(selected)}</div><small class="muted">${linkedProcess?'O vínculo já foi selecionado e será salvo com a meta.':'Opcional. Clique em um município para abrir os projetos e núcleos disponíveis.'}</small>`;const assoc=form.querySelector('#metaAssocType')?.closest('.field');assoc?.after(wrap)||respField?.before(wrap)||form.appendChild(wrap);
 wireProjectTabs(wrap);
 const search=wrap.querySelector('.meta-project-search');search.oninput=()=>{const q=norm(search.value);wrap.querySelectorAll('.meta-project-group').forEach(g=>{const municipalityMatch=norm(g.dataset.municipio).includes(q);let visible=0;g.querySelectorAll('.meta-project-item').forEach(i=>{const show=!q||municipalityMatch||norm(i.textContent).includes(q);i.hidden=!show;if(show)visible++});g.hidden=!!q&&!visible;if(q&&visible)setGroupOpen(g,true);else if(!q&&g.querySelectorAll('input[name="metaProjeto"]:checked').length===0)setGroupOpen(g,false)})};
 form.addEventListener('submit',()=>{const ids=[...form.querySelectorAll('input[name="metaProjeto"]:checked')].map(x=>x.value);const title=form.elements.titulo?.value?.trim()||'';const expected=currentMetaId;setTimeout(()=>persistLinks(expected,title,ids),700)},true);
}
async function persistLinks(expectedId,title,ids){if(!sb)return;let metaId=expectedId;if(!metaId&&title){const u=bridge?.currentUser?.id;let q=sb.from('metas').select('id').eq('titulo',title).order('updated_at',{ascending:false}).limit(1);if(u)q=q.eq('created_by',u);const r=await q.maybeSingle();metaId=r.data?.id||null;}if(!metaId)return;currentMetaId=metaId;const del=await sb.from('meta_projetos').delete().eq('meta_id',metaId);if(del.error){console.warn(del.error);return}if(ids.length){const rows=ids.map(id=>{const p=processos.find(x=>x.id===id);return{meta_id:metaId,processo_id:id,crm_projeto_id:p?.crm_projeto_id||null,created_by:bridge?.currentUser?.id||null}});const ins=await sb.from('meta_projetos').insert(rows);if(ins.error){console.warn(ins.error);return}}await syncMeta(metaId)}
async function injectProcessMetas(){const modal=document.querySelector('#processModal');if(!modal||modal.dataset.metasInjected||!currentProcessId)return;modal.dataset.metasInjected='1';const body=modal.querySelector('.modal-body');if(!body)return;const lr=await sb.from('meta_projetos').select('meta_id').eq('processo_id',currentProcessId);const ids=(lr.data||[]).map(x=>x.meta_id);const section=document.createElement('section');section.className='process-meta-section';const add=window.ERPMetasV2?.canCreate?.()?'<button type="button" class="btn secondary process-add-meta">+ Metas</button>':'';if(!ids.length){section.innerHTML=`<div class="process-meta-heading"><h4>Metas vinculadas</h4>${add}</div><div class="empty">Nenhuma meta vinculada a este processo.</div>`;}else{const [mr,kr]=await Promise.all([sb.from('metas').select('id,titulo,status,prazo,observacoes').in('id',ids),sb.from('meta_checklist').select('meta_id,titulo,concluido').in('meta_id',ids)]);section.innerHTML=`<div class="process-meta-heading"><h4>Metas vinculadas</h4>${add}</div><div class="process-meta-list">${(mr.data||[]).map(m=>{const ck=(kr.data||[]).filter(x=>x.meta_id===m.id),done=ck.filter(x=>x.concluido).length,pct=ck.length?Math.round(done/ck.length*100):0;return`<article><div><strong>${esc(m.titulo)}</strong><span>${esc(m.status||'Em andamento')}${m.prazo?' · prazo '+new Date(m.prazo+'T12:00:00').toLocaleDateString('pt-BR'):''}</span></div><div class="process-meta-progress"><i style="width:${pct}%"></i></div><small>${ck.length?`${done}/${ck.length} desdobramentos concluídos`:'Sem desdobramentos'}</small></article>`}).join('')}</div>`;}section.querySelector('.process-add-meta')?.addEventListener('click',()=>window.ERPMetasV2?.createForProcess?.(currentProcessId));const historyTitle=[...body.querySelectorAll('h4')].find(x=>/Histórico de etapas/i.test(x.textContent));historyTitle?.before(section)||body.appendChild(section)}
function scheduleSyncFromMetaAction(target){const card=target.closest?.('[data-meta-card]');const id=card?.dataset?.metaCard||currentMetaId;if(id)setTimeout(()=>syncMeta(id),1000)}
function clickCapture(e){const t=e.target;const card=t.closest?.('[data-meta-card]');if(card)currentMetaId=card.dataset.metaCard;if(t.closest?.('#metaNew,#metaActiveNew,#metaUserNew,#osDetailMeta'))currentMetaId=null;const pc=t.closest?.('.process-card[data-process-id]');if(pc){currentProcessId=pc.dataset.processId;setTimeout(injectProcessMetas,80)}if(t.closest?.('[data-request-meta],[data-approve-meta],#metaDelete,#addMetaChecklistItem,.meta-check-delete'))scheduleSyncFromMetaAction(t)}
function changeCapture(e){if(e.target.closest?.('.meta-check-item input[type="checkbox"]'))scheduleSyncFromMetaAction(e.target)}
function observe(){new MutationObserver(()=>{const f=document.querySelector('#metaV2Form');if(f)enhanceForm(f);if(document.querySelector('#processModal'))injectProcessMetas()}).observe(document.documentElement,{childList:true,subtree:true})}
function start(){bridge=window.ERPIntegralBridge;if(!bridge)return;sb=bridge.sb;loadProcessos();window.addEventListener('erp-meta-for-process',e=>{currentMetaId=null;currentProcessId=e.detail?.processId||null;});document.addEventListener('click',clickCapture,true);document.addEventListener('change',changeCapture,true);observe()}
window.addEventListener('erp-bridge-ready',start,{once:true});if(window.ERPIntegralBridge)start();
})();