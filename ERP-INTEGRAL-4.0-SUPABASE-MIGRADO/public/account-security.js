(() => {
'use strict';
const cfg=window.ERP_SUPABASE||{};
const securitySb=window.supabase?.createClient(cfg.url,cfg.publishableKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
let adminCache=null;
function escSecurity(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
async function isCurrentAdmin(){if(adminCache!==null)return adminCache;try{const {data:{user}}=await securitySb.auth.getUser();if(!user)return adminCache=false;const {data}=await securitySb.from('profiles').select('tipo,ativo').eq('id',user.id).maybeSingle();return adminCache=!!data&&data.ativo!==false&&data.tipo==='Administrador'}catch{return adminCache=false}}
function passwordFields(title,intro){return `<div class="password-intro">${intro}</div><form id="passwordForm" class="form-grid"><div class="field full"><label>Nova senha</label><input name="password" type="password" autocomplete="new-password" minlength="8" required></div><div class="field full"><label>Confirmar nova senha</label><input name="confirm" type="password" autocomplete="new-password" minlength="8" required></div><div id="passwordStatus" class="field full muted">Use pelo menos 8 caracteres.</div></form>`}
function modalShell(title,body){document.querySelector('#passwordModal')?.remove();const el=document.createElement('div');el.id='passwordModal';el.className='modal-backdrop';el.innerHTML=`<section class="modal"><header class="modal-head"><h3>${escSecurity(title)}</h3><button id="passwordClose" class="btn icon ghost">×</button></header><div class="modal-body">${body}</div><footer class="modal-foot"><button id="passwordCancel" class="btn ghost">Cancelar</button><button id="passwordSave" class="btn">Salvar nova senha</button></footer></section>`;document.body.appendChild(el);const close=()=>el.remove();el.querySelector('#passwordClose').onclick=close;el.querySelector('#passwordCancel').onclick=close;el.onclick=e=>{if(e.target===el)close()};el.querySelector('#passwordSave').onclick=()=>el.querySelector('#passwordForm').requestSubmit();return {el,close}}
function validate(el){const fd=new FormData(el.querySelector('#passwordForm')),password=String(fd.get('password')||''),confirm=String(fd.get('confirm')||''),status=el.querySelector('#passwordStatus');if(password.length<8){status.textContent='A nova senha precisa ter pelo menos 8 caracteres.';return null}if(password!==confirm){status.textContent='As senhas informadas não são iguais.';return null}return {password,status}}
function openPasswordModal(){const {el,close}=modalShell('Alterar minha senha',passwordFields('Alterar minha senha','Defina uma nova senha para sua conta. A alteração também será usada no Financeiro.'));el.querySelector('#passwordForm').onsubmit=async e=>{e.preventDefault();const v=validate(el);if(!v)return;const save=el.querySelector('#passwordSave');save.disabled=true;save.textContent='Alterando...';v.status.textContent='Atualizando sua senha...';try{if(!securitySb)throw new Error('Conexão com o Supabase indisponível.');const {data:{session}}=await securitySb.auth.getSession();if(!session)throw new Error('Sua sessão expirou. Entre novamente no ERP.');const {error}=await securitySb.auth.updateUser({password:v.password});if(error)throw error;v.status.textContent='Senha alterada com sucesso.';save.textContent='Concluído';setTimeout(close,1200)}catch(err){v.status.textContent=`Não foi possível alterar a senha: ${err.message||'erro desconhecido'}`;save.disabled=false;save.textContent='Salvar nova senha'}}}
async function openAdminPasswordModal(userId,userName){if(!await isCurrentAdmin())return alert('Apenas administradores podem redefinir senhas de outros usuários.');const {el,close}=modalShell(`Alterar senha — ${userName}`,passwordFields('',`Você está redefinindo a senha de <b>${escSecurity(userName)}</b>. O usuário poderá entrar com a nova senha imediatamente.`));el.querySelector('#passwordForm').onsubmit=async e=>{e.preventDefault();const v=validate(el);if(!v)return;const save=el.querySelector('#passwordSave');save.disabled=true;save.textContent='Alterando...';v.status.textContent='Atualizando senha do usuário...';try{const {data:{session}}=await securitySb.auth.getSession();if(!session)throw new Error('Sua sessão expirou.');const r=await fetch('/api/admin-user-password',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({userId,password:v.password})});const data=await r.json().catch(()=>({}));if(!r.ok||!data.ok)throw new Error(data.error||'Falha ao atualizar senha.');v.status.textContent='Senha do usuário alterada com sucesso.';save.textContent='Concluído';setTimeout(close,1200)}catch(err){v.status.textContent=`Não foi possível alterar a senha: ${err.message||'erro desconhecido'}`;save.disabled=false;save.textContent='Salvar nova senha'}}}
function installSecurityButton(){const foot=document.querySelector('.sidebar-foot');if(!foot||foot.querySelector('#changeMyPassword'))return;const logout=foot.querySelector('#logout');if(!logout)return;const btn=document.createElement('button');btn.id='changeMyPassword';btn.className='security-link-btn';btn.type='button';btn.textContent='Segurança da conta · alterar senha';btn.onclick=openPasswordModal;foot.insertBefore(btn,logout)}
async function installAdminButtons(){if(!await isCurrentAdmin())return;document.querySelectorAll('[data-edit-user]').forEach(edit=>{const actions=edit.closest('.actions');if(!actions||actions.querySelector(`[data-admin-password="${edit.dataset.editUser}"]`))return;const row=edit.closest('tr'),name=row?.querySelector('td')?.textContent?.trim()||'Usuário';const btn=document.createElement('button');btn.type='button';btn.className='btn small secondary';btn.dataset.adminPassword=edit.dataset.editUser;btn.textContent='Senha';btn.title='Alterar senha deste usuário';btn.onclick=()=>openAdminPasswordModal(edit.dataset.editUser,name);actions.prepend(btn)})}
const observer=new MutationObserver(()=>{installSecurityButton();installAdminButtons()});observer.observe(document.documentElement,{childList:true,subtree:true});installSecurityButton();installAdminButtons();window.ERPAccountSecurity={openPasswordModal,openAdminPasswordModal};
})();
